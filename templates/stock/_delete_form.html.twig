{# Stock Delete Form Template #}
<form method="post" action="{{ path('stock_delete', {'id': stock.id}) }}" onsubmit="return confirm('Are you sure?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ stock.id) }}">
    <button type="submit" class="btn btn-danger">Delete</button>
</form>
